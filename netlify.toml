# Netlify deploy config for Lumen Light.
#
# The static Vite build is served from the CDN; the /api/* endpoints run as
# serverless functions (netlify/functions/*.mts) that reuse server/backend.ts,
# so API keys stay server-side. Set these env vars in the Netlify UI
# (Site settings -> Environment variables) — same names as .env.local:
#   GEMINI_API_KEY (required for live voice + images), optionally
#   GEMINI_LIVE_MODEL / GEMINI_LIVE_VOICE, ONE of TAVILY_API_KEY /
#   BRAVE_API_KEY, and THUM_IO_KEY.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"
  directory = "netlify/functions"

# Serve the PWA manifest with the correct type, and keep the service worker
# uncached so clients pick up new deploys promptly.
[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
